1.DOM
DOM是一组都对象的集合

2.DOM级别
不同的级别，定义的标准不一样，所实现的功能不同
即使同一级别，不同浏览器之间也有差异
并非所有的DOM功能，浏览器都实现
使用js库去执行这些操作，消除这些差异，例如jQuery
使用if语句测试浏览器是否存在此功能

3.DOM入口
document对象
location对象
window对象
History对象
Screen对象
HTMLElement对象
Text对象
CSSStyleDeclaration对象
DOM事件系统