1.字符字面值由单引号包围，'a'，字符串字面值由双引号包围，"A"

2.编译器会在字符串字面值后默认加上空字符('\0')，因此长度会加1

3.字符串字面值的类型实际上是由常量字符构成的数组

4.如果两个字符串字面值位子紧邻且仅由空格、缩进和换行符分隔，则它们实际上是一个整体
例如：
std::cout << "kldsajfkljalkjflka"
              << "fdjakljfkldasjklf"
              << std::endl;
输出的结果为一行