定义：包含yield语句的函数都被称为生成器

特点：
1.yield可以返回多个值，每次一个
2.yield没返回一个值后，都要被冻结，等待被唤醒
3.被唤醒后将从停止的地方开始继续
4.生成器被调用时，不会执行函数内代码，只返回一个迭代器
5.在被请求值时，才会被执行代码，在遇到yield或return时停止，yield回一个值，return停止执行生成器
6.由于yield可以停止，所以可以在用next启动后，用send方法给yield传递参数

生成器推导：
1.在圆括号内，与列表推导相似，但是返回生成器
2.在既有圆括号内（如函数调用）使用生成器推导时，无需再加圆括号