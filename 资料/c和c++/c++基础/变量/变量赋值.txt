1.变量在赋值时，会将值转换为声明的数据类型
int a = 3.14; 结果i = 3

2.使用列表初始化赋值，如果值与声明的数据类型，编译器会报错
int a = {3.14} 或 int a{3.14}编译器会报错，防止数据信息丢失

3.c++中不支持链式赋值
int a = b = 3;会发生错误

4.在函数体之外的地方，给内置数据类型初始化，如果未指定初始值，会默认为0
如果在函数体之内，没有初始化，其值未定义（例如int，会给出一个不确定的值）
std::string itme;会出一个空值