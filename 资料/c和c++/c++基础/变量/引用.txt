1.语法：
数据类型符 &变量名 = 对象（对象的数据类型必须与变量的数据类型相同）
例如:
int i = 10, &ri = i;

2.引用的变量与引用的对象都指向同一个值，引用的变量与引用的对象是同一个对象，
只是名字不同，一个变化，另外一个也会变化

3.引用的变量的类型必须与引用的对象的类型相同

4.引用的变量必须有初始值

5.引用的变量可以作为对象赋值给其他引用的变量，int &ri2 = ri，此时三个对象都是同一个，
只是名字不同