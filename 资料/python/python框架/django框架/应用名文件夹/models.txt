from django.db import models 

# Create your models here.
# 建立数据模型，模型由CharField和TextField两种数据模型构成
class Article(models.Model):
    title = models.CharField(max_length = 30)
    content = models.TextField()
#设置在后台管理页面中，对象显示的内容
    def __str__(self):
	return 'self.title'